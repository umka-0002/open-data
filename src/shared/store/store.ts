import { atom } from "jotai";

export const dataTypeAtom = atom<string | null>(null);
export const categoriesAtom = atom<string | null>(null);
export const dateRangeAtom = atom<string | null>(null);
